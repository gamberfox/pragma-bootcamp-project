server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost/stock
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      host: localhost
      port: 27017
      database: stock